name: pymablock-test

channels:
  - conda-forge

dependencies:
  - python=3.11
  - python-mumps
  - sympy
  - numpy
  - scipy
  - pytest-cov
  - pytest-randomly
  - pytest-repeat
  - pytest-regressions
  - pip
  - pip:
    - pytest-ruff
